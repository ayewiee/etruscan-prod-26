## Матрица соответствия `задание–критерий–реализация`

_PARTIALLY OR FULLY AI GENERATED (but reviewed and refined by a human <3)_

---

Таблица помогает жюри быстро найти, **где именно реализовано требование**, **как его проверить** и **какие данные нужны**.  
ID заданий отсылают к разделам `docs/initial/TASK.md`, ID критериев — к `docs/initial/CRITERIA.md`.  
Каждый критерий `B1-1..B10-2`, `FX-1`, `FX-2` имеет **отдельную строку**.

| ID критерия | ID задания | Проблема/риск | Где реализовано | Как проверяется | Какие данные нужны | Статус |
|---|---|---|---|---|---|---|
| `B1-1` | `D.4` | Запуск системы требует скрытых знаний; предусловия неочевидны и часть зависимостей может быть не поднята. | `docs/RUNBOOK.md` (раздел 1), `docker-compose.yml` | Открыть runbook, проверить список предусловий (Docker, Compose, порт 80) и переменные окружения; убедиться, что дополнительных шагов вне документа не требуется. | Локальная машина с Docker/Compose, доступ к репозиторию. | `готово` |
| `B1-2` | `D.4` | Без точных команд запуска жюри тратит время на догадки и может поднять систему в неправильной конфигурации. | `docs/RUNBOOK.md` (раздел 2), `docker-compose.yml` | Выполнить из корня репозитория команды из runbook: `docker compose up -d`, затем проверить, что сервисы поднялись (`docker ps`) и отвечают на `/api/v1/ready`. | Docker / Docker Compose, свободный порт `80`. | `готово` |
| `B1-3` | `D.4` | Если запуск требует неописанных ручных шагов (миграции, создание пользователей), проверка становится невоспроизводимой. | Миграции и сиды в `src/db/migrations`, сценарий запуска в `docs/RUNBOOK.md` | Поднять стек строго по runbook и убедиться, что миграции/сид‑данные применяются автоматически (admin‑пользователь доступен без дополнительных действий). | Docker/Compose, образы Go/Postgres/Redis, сид‑данные из миграций. | `готово` |
| `B1-4` | `D.4` | Система может подняться частично: процесс жив, но запросы не обслуживаются. | `/api/v1/health`, `/api/v1/ready` в `internal/app/routes.go`, `docs/OPERATIONS.md` (раздел 1) | После `docker compose up -d` периодически опрашивать `/api/v1/health` и `/api/v1/ready`; убедиться, что `health` начинает отвечать `200` сразу, а `ready` не позже 180 секунд и только после инициализации зависимостей. | Запущенный стек, curl либо браузер. | `готово` |
| `B1-5` | `D.5` | Отсутствие одного проверяемого happy‑path не позволяет убедиться, что все части платформы связаны корректно. | `docs/RUNBOOK.md` (раздел 4), e2e‑тесты `tests/e2e` | Пройти по runbook путь `flag → experiment → decide → track → report` и убедиться, что все шаги выполняются и дают ожидаемый результат. Для автоматической проверки — запустить `make test-e2e`. | Docker и Go toolchain для e2e‑тестов; HTTP‑клиент для ручного сценария. | `готово` |
| `B2-1` | `3.4` | При отсутствии активного эксперимента платформа может вернуть не `default`, ломая контрольный сценарий. | `internal/usecases/decide.go`, `internal/domain/models/flag.go`, `docs/RUNBOOK.md` (4.5) | Создать флаг без активного эксперимента и вызвать `POST /api/v1/decide` — убедиться, что `value` равно `defaultValue`, а участие в эксперименте не регистрируется. | Seed‑флаг `purchase_button_text`, без запущенного эксперимента; userId `client123`. | `готово` |
| `B2-2` | `3.4` | Пользователь вне таргетинга может неожиданно получить экспериментальный вариант, и результат эксперимента станет некорректным. | `internal/usecases/decide.go` (применение `TargetingRule`), `internal/domain/bucketing`, `docs/ARCHITECTURE.md` (DecideUseCase) | Создать эксперимент с таргетингом (например, `region != 'US' AND version >= '1.2.3'`) и вызвать `/decide` с контекстом, не проходящим правило; убедиться, что возвращается `defaultValue`. | Эксперимент из runbook (4.3), контексты с `region = 'US'` или `version < '1.2.3'`. | `готово` |
| `B2-3` | `3.4` | При выполнении всех условий пользователь может продолжать получать `default`, и эксперимент в реальности не проводится. | `internal/usecases/decide.go`, `internal/domain/bucketing`, `docs/RUNBOOK.md` (4.5) | Для пользователя, попадающего в аудиторию и соответствующего таргетингу, многократно вызвать `/decide` и убедиться, что возвращается одно из значений вариантов, а не `default`. | Эксперимент `purchase_button_text` с 100% аудиторией и подходящим контекстом (`region="RU"`, `version="1.3"`, `premium=true`). | `готово` |
| `B2-4` | `3.4` | Отсутствие детерминизма (при тех же условиях разные варианты) ломает UX и статистику. | `internal/domain/bucketing` (`HashAndBucket`, `ChooseVariant`), `internal/usecases/decide.go` | Для фиксированного `userId`, `flagKey` и конфигурации эксперимента многократно вызывать `/decide`; убедиться, что возвращается одно и то же значение (при неизменных данных). | Любой запущенный эксперимент, один `userId`, фиксированный контекст. | `готово` |
| `B2-5` | `3.4` | Если веса вариантов игнорируются, эксперимент не даёт ожидаемого распределения трафика. | `internal/domain/bucketing` (распределение по весам), `internal/usecases/decide.go` | Написать скрипт, который делает серию запросов `/decide` (несколько тысяч) и считает долю каждого варианта; сравнить с настроенными весами. | Эксперимент с весами (например 25/25/50 из runbook); небольшой прогон нагрузки. | `готово` |
| `B3-1` | `2.4/2.5` | Без перехода `draft -> in_review` нельзя отделить черновики от кандидатов к запуску. | `internal/usecases/experiment_lifecycle.go`, `internal/api/handlers/experiment.go` (`SendOnReview`), `docs/RUNBOOK.md` (4.4) | Создать эксперимент в статусе `DRAFT`, вызвать `POST /experiments/{id}/sendOnReview`, затем `GET /experiments/{id}` и убедиться, что статус изменился на `ON_REVIEW`. | Admin‑token, seed‑эксперимент из runbook. | `готово` |
| `B3-2` | `2.4/2.5` | При выполнении условий ревью эксперимент может так и остаться в статусе `ON_REVIEW`, блокируя запуск. | `internal/usecases/experiment_review.go`, `internal/api/handlers/experiment.go` (`Approve`) | Настроить approver‑группу и дважды вызвать `POST /experiments/{id}/approve` (admin + approver); убедиться, что статус стал `APPROVED`. | Admin‑ и approver‑пользователи, approver‑группа, эксперимент в статусе `ON_REVIEW`. | `готово` |
| `B3-3` | `2.4/2.5` | Эксперимент может быть запущен без достаточного числа одобрений, что нарушает политику безопасности. | `internal/usecases/experiment_lifecycle.go` (`Launch`), бизнес‑правила ревью, ADR‑007 | Попытаться запустить эксперимент через `POST /experiments/{id}/launch` до достижения порога approve; ожидать ошибку; затем повторить после необходимого числа одобрений — запуск должен пройти. | Конфигурация `DEFAULT_MIN_APPROVALS=2`, один admin и один approver. | `готово` |
| `B3-4` | `2.4/2.5` | Неправильные переходы статусов (например, `FINISHED -> DRAFT`) делают историю эксперимента недостоверной. | `internal/domain/models/experiment_status.go`, `internal/usecases/experiment_lifecycle.go`, unit‑тесты в `internal/domain/models` | Попробовать выполнить запрещённые переходы через API (например, повторный `sendOnReview` после `LAUNCHED`); убедиться, что возвращается ошибка, а статус не меняется. | HTTP‑клиент, эксперимент в соответствующих состояниях. | `готово` |
| `B3-5` | `0.3` / `2.4` | Если ревью могут проводить любые пользователи, процесс согласования теряет смысл. | Роли и группы в `internal/domain/models/user.go`, `internal/usecases/experiment_review.go`, `docs/PERMISSIONS.md` | Попробовать вызвать `/experiments/{id}/approve` под пользователем без роли `APPROVER`/`ADMIN`; ожидать ошибку доступа. | Разные пользователи (admin, approver, viewer), настроенные approver‑группы. | `готово` |
| `B4-1` | `4.2` | Принятие событий с неверными типами полей ломает метрики и отчёты. | `internal/usecases/event.go` (валидация типов), `internal/api/dto/event.go`, `docs/METRICS_AND_GUARDRAILS.md` | Отправить событие через `POST /api/v1/track` с полем неверного типа (например, `timestamp` строкой); убедиться, что оно попадает в `rejected` с описанием ошибки. | Любой валидный `decisionId`, один заведомо невалидный элемент в пачке событий. | `готово` |
| `B4-2` | `4.2` | Принятие событий без обязательных полей делает атрибуцию невозможной. | `internal/usecases/event.go` (проверка обязательных полей), `internal/api/dto/event.go` | Отправить событие без `eventTypeKey` или `eventId`; запрос должен вернуть `rejected > 0` и соответствующее сообщение в `errors`. | `decisionId` из `/decide`, JSON‑пачка с неполным событием. | `готово` |
| `B4-3` | `4.3` | Дубликаты событий могут раздувать метрики и ломать отчёты. | `internal/repository/event.go` (проверка `ExistsByTypeAndClientID`), `internal/usecases/event.go` | Отправить событие с `eventId=X` дважды; во втором ответе ожидать увеличение `duplicates` и отсутствие влияния на метрики. | Один `eventId`, один `decisionId`, два вызова `/track`. | `готово` |
| `B4-4` | `4.4` | Если экспозиция не связана с `decisionId`, конверсии нельзя честно привязать к варианту. | `internal/domain/models/event.go`, сохранение `decisionId` в `EventRepository`, `docs/ARCHITECTURE.md` (критический путь `decide → track`) | Проверить, что событие `exposure` сохраняется с `decisionId`, полученным от `/decide`, и участвует в отчётах и guardrails; без этого события конверсии не учитываются. | Решение `/decide` с `decisionId`, валидное событие `exposure`. | `готово` |
| `B4-5` | `4.4` | Учитывание конверсий без подтверждённой экспозиции делает метрики мусором. | Логика атрибуции в `internal/usecases/metric_compute.go`, описание в `docs/METRICS_AND_GUARDRAILS.md` | Отправить конверсию с `decisionId`, для которого нет события `exposure`, затем построить отчёт и убедиться, что такая конверсия не попадает в целевые метрики. | Один `decisionId` без `exposure`, одно событие конверсии, затем вызов `GET /experiments/{id}/report`. | `готово` |
| `B5-1` | `6.1/6.2` | Guardrail без явно указанной метрики неясен и не может быть корректно пересчитан. | `internal/domain/models/guardrail.go`, `internal/api/dto/experiment.go` (`GuardrailRequest`), `docs/METRICS_AND_GUARDRAILS.md` | Попробовать создать/обновить эксперимент с guardrail без `metricKey`; ожидать ошибку валидации. | Тело `CreateUpdateExperimentRequest` с пустым `metricKey`. | `готово` |
| `B5-2` | `6.1/6.2` | Guardrail без порога не определяет, когда срабатывать. | То же, дополнительные проверки порога в домене и usecase | Попробовать создать guardrail без `threshold` или с `0`, если это невалидно по бизнес‑правилам; убедиться в отклонении запроса. | HTTP‑запрос с guardrail‑объектом без `threshold`. | `готово` |
| `B5-3` | `6.1/6.2` | При реальной деградации guardrail может не сработать и не остановить эксперимент. | `internal/usecases/guardrail_runner.go`, `internal/usecases/metric_compute.go`, `docs/ARCHITECTURE.md` | Настроить guardrail на `error_rate`, сгенерировать поток `error`‑событий выше порога и дождаться следующего запуска GuardrailRunner; убедиться, что правило фиксируется как сработавшее. | Эксперимент с guardrail‑метрикой, события `error` с высоким значением ratio. | `готово` |
| `B5-4` | `6.1/6.2` | Guardrail может быть зафиксирован как сработавший, но статус эксперимента не изменится. | `internal/usecases/guardrail_runner.go` (изменение статуса), `internal/usecases/experiment_lifecycle.go` | После срабатывания guardrail проверить статус эксперимента (`GET /experiments/{id}`) — он должен стать `PAUSED` или сценарий `ROLLBACK` должен завершить эксперимент. | Те же данные, что и для B5‑3. | `готово` |
| `B5-5` | `6.1/6.2` | Без истории срабатываний guardrails нельзя объяснить, почему эксперимент остановился. | `internal/repository/guardrail.go` (таблица триггеров), структура логов в `docs/OPERATIONS.md` | После срабатывания guardrail получить историю триггеров (через репозиторий/эндпоинт или по логам) и убедиться, что там указаны метрика, порог, окно, действие, фактическое значение и время. | Эксперимент с guardrail, несколько срабатываний при деградации. | `готово` |
| `B5-6` | `3.6/3.7` | Один и тот же пользователь может постоянно быть в экспериментальных ветках, что портит опыт и смещает выборку. | `internal/repository/cache/participation.go` (`ParticipationTracker`), `internal/usecases/decide.go`, `docs/ARCHITECTURE.md` | Запустить ≥3 экспериментов и многократно вызывать `/decide` для одного `userId` по разным флагам; убедиться, что только первые два дают вариант, остальные возвращают `defaultValue`, а в Redis по `user_active_exps:<userId>` не более лимита. | Redis из `docker-compose.yml`, несколько экспериментов и один повторяющийся `userId`. | `готово` |
| `B6-1` | `5.2` | Без фильтра по периоду нельзя честно фиксировать окно эксперимента; любой отчёт становится “плавающим”. | `internal/api/handlers/report.go`, `internal/usecases/report.go`, `docs/RUNBOOK.md` (4.7) | Вызвать `GET /experiments/{id}/report` с разными значениями `from/to` и убедиться, что отчёт меняется в соответствии с окном (а не всегда считает “за всё время”). | Эксперимент с событиями в разных днях, несколько запросов `/report` с разными окнами. | `готово` |
| `B6-2` | `5.3` | Если отчёт доступен только в агрегате по эксперименту, нельзя сравнить варианты между собой. | `internal/usecases/report.go` (разрез по вариантам), `docs/ARCHITECTURE.md` | В ответе `/experiments/{id}/report` убедиться в наличии массива `variants`, где по каждому варианту есть отдельный набор метрик. | Один эксперимент с ≥2 вариантами и событиями. | `готово` |
| `B6-3` | `5.4` | Если отчёт игнорирует часть выбранных метрик, продукт не видит полной картины. | `internal/usecases/report.go`, `internal/usecases/metric_compute.go`, `docs/METRICS_AND_GUARDRAILS.md` | Привязать к эксперименту несколько метрик, затем проверить, что в отчёте для каждого варианта присутствуют значения всех перечисленных в конфигурации метрик. | Эксперимент с несколькими метриками в `metricKeys`, события нужных типов. | `готово` |
| `B6-4` | `5.4/2.6` | Если исход эксперимента не зафиксирован структурированно, команда забудет, что было решено, и будет повторять тесты. | `internal/usecases/experiment_lifecycle.go` (`Finish`), `internal/api/dto/experiment.go`, `docs/RUNBOOK.md` (4.8) | Вызвать `POST /experiments/{id}/finish` с различными `outcome` (`ROLLOUT`, `ROLLBACK`, `NO_EFFECT`) и проверить, что они сохраняются и отображаются при `GET /experiments/{id}`. | Завершённый эксперимент, несколько сценариев исхода. | `готово` |
| `B6-5` | `5.4/2.6` | Решение без комментария не даёт контекста: через полгода непонятно, почему вариант приняли или отклонили. | То же, поле `OutcomeComment` в домене, `docs/LEARNINGS` (если есть) | При вызове `finish` передать содержательный `comment` и убедиться, что он сохраняется и отображается в API/UI. | Любой завершённый эксперимент, одно текстовое обоснование. | `готово` |
| `B7-1` | `D.4/D.6` | Непрозрачный нейминг сущностей и эндпоинтов делает систему непонятной без “внутренних знаний”. | `docs/ARCHITECTURE.md`, `README.md`, доменные модели в `internal/domain/models` | Проверить, что названия флагов, экспериментов, метрик, событий и эндпоинтов отражают назначение (напр. `/decide`, `/track`, `GuardrailRunner` и т.п.) и согласуются с документацией. | Только репозиторий и документация. | `готово` |
| `B7-2` | `D.4/D.6` | Отсутствие явных границ модулей затрудняет сопровождение и оценку архитектуры. | C4‑диаграммы в `docs/ARCHITECTURE.md`, структура `internal/app`, `internal/usecases`, `internal/domain`, `internal/repository` | По C4 Container и Component‑диаграммам убедиться, что обязанности модулей разделены (API, usecases, domain, repository, cache) и это соответствует структуре кода. | Только репозиторий и архитектурные доки. | `готово` |
| `B7-3` | `D.6` | Без матрицы соответствия жюри сложно быстро понять, где реализован каждый критерий. | Этот файл `docs/TRACEABILITY_MATRIX.md` | Использовать матрицу во время демо, чтобы по ID критерия сразу найти: файлы, сценарий проверки и нужные данные. | Репозиторий и этот документ. | `готово` |
| `B7-4` | `D.4/D.6` | Если ключевые архитектурные решения не зафиксированы, их невозможно обсуждать и защищать. | `docs/ARCHITECTURE.md`, ADR‑001..ADR‑007 | Просмотреть перечисленные ADR и раздел “Ограничения и упрощения” в `ARCHITECTURE.md`; убедиться, что решения согласуются с кодом и демо. | Репозиторий и ADR. | `готово` |
| `B7-5` | `D.4/D.6` | Без C4 Context‑диаграммы непонятны границы системы и внешние акторы. | C4 L1 в `docs/ARCHITECTURE.md` | Открыть раздел “C4 Context (L1)” и убедиться, что там показаны клиентские приложения, админские пользователи, операторы и внешние зависимости (Postgres, Redis, Telegram). | Только документация. | `готово` |
| `B7-6` | `D.4/D.6` | Без C4 Container‑диаграммы трудно понять разбиение на сервисы/контейнеры. | C4 L2 в `docs/ARCHITECTURE.md` | Убедиться, что диаграмма показывает Etruscan API, Postgres, Redis, Telegram/SMTP и связи между ними. | Документация. | `готово` |
| `B7-7` | `D.4/D.6` | Без ограниченной C4 Component‑диаграммы по критичному пути сложно объяснить, как реализован `decide → event → report/guardrail`. | C4 L3 в `docs/ARCHITECTURE.md` (DecideHandler, EventsUseCase, ReportUseCase, GuardrailRunner) | Пройти по диаграмме L3 и сопоставить её с файлами `internal/api/handlers/*`, `internal/usecases/*`, `internal/repository/*`. | Документация и код. | `готово` |
| `B7-8` | `D.4` | Отсутствие карты репозитория замедляет ориентирование по проекту. | `README.md` (карта репозитория), ссылка на `docs/ARCHITECTURE.md`, `docs/TESTING.md`, `docs/RUNBOOK.md` | Открыть `README.md` и убедиться, что там описаны основные папки (`src/internal/...`, `docs/`, `ui/...`) и куда идти за ключевыми сценариями. | Репозиторий. | `готово` |
| `B7-9` | `D.6` | Если ограничения и упрощения не зафиксированы, они всплывают только в вопросах и создают недоверие. | Раздел “Ограничения и упрощения” в `docs/ARCHITECTURE.md`, дополнительно — замечания в `docs/OPERATIONS.md` и ADR | Просмотреть список ограничений (например, упрощённая email‑интеграция, хранение событий в одной БД) и убедиться, что они согласуются с кодом и демо. | Документация. | `готово` |
| `B8-1` | `D.5` | Отсутствие негативных тестов скрывает важные ошибки валидации и граничные случаи. | Unit‑тесты в `internal/domain/models/*_test.go`, `internal/usecases/*_test.go`, описание в `docs/TESTING.md` | Запустить `make test-unit` и в отчёте по тестам найти негативные сценарии (невалидные статусы, ошибки валидации, неверные переходы). | Go toolchain, доступ к `src/`. | `готово` |
| `B8-2` | `D.5` | Без интеграционных/e2e‑тестов критичный путь может ломаться незаметно. | E2E‑тесты `tests/e2e`, цели `test-e2e` и `test` в `src/Makefile` | Выполнить `make test-e2e` или `make test` и убедиться, что e2e‑набор проходит сквозные сценарии (подъём БД/Redis, миграции, health/readiness, auth, happy‑path). | Go, Docker, образы Postgres/Redis. | `готово` |
| `B8-4` | `D.5` | Без воспроизводимого отчёта по покрытию сложно оценить полноту тестов. | Цели `coverage` и `coverage-html` в `src/Makefile`, `docs/TESTING.md` | Выполнить `make coverage` и `make coverage-html`, открыть `coverage.html` и убедиться, что команды воспроизводимы и показывают суммарное покрытие. | Go toolchain, доступ к `src/`. | `готово` |
| `B9-1` | `3.7/B9` | Readiness‑проверка может врать, что сервис готов, хотя зависимости ещё не поднялись. | `/api/v1/ready` в `internal/app/routes.go`, `docs/OPERATIONS.md` (1.2) | Поднять стек и опрашивать `/ready` в первые 180 секунд; убедиться, что до готовности зависимостей код не `200`, а после инициализации становится `200`. | Docker/Compose, curl. | `готово` |
| `B9-2` | `3.7/B9` | Без liveness‑проверки оркестратор не сможет перезапустить “зависший” процесс. | `/api/v1/health` в `internal/app/routes.go`, `docs/OPERATIONS.md` | Смоделировать сбой (например, временно убить API‑контейнер) и посмотреть, как `/health` используется как liveness‑probe. | Контейнерная среда (Docker/Compose или оркестратор). | `готово` |
| `B9-3` | `3.7/B9` | Отсутствие экспортируемых метрик делает систему “чёрным ящиком” под нагрузкой. | `docs/OPERATIONS.md` (раздел 2), `internal/usecases/metric_compute.go` | На демо показать точку экспорта продуктовых метрик (через отчёты/guardrails) и технических (через логи/обёртки HTTP); убедиться, что по ним можно мониторить трафик и ошибки. | Запущенный стек, примеры логов и отчётов. | `готово` |
| `B9-4` | `3.7/B9` | Неструктурированные логи сложнее собирать и анализировать; теряется контекст инцидентов. | `internal/app/logger/*`, `docs/OPERATIONS.md` (раздел 3) | Посмотреть пример логов (`logger.NewZapLogger`, `ZapLoggerMiddleware`): формат JSON‑подобный, есть поля уровня, пути, статуса, request_id, метрик guardrails. | Доступ к логам контейнера API. | `готово` |
| `B9-6` | `3.7/B9` | Поведение при росте данных и нагрузки может резко деградировать, если оно не продумано заранее. | `docs/OPERATIONS.md` (разделы 4.1–4.2), индексация таблиц и кеши Redis в `docs/ARCHITECTURE.md` | По описанию убедиться, что горячие пути (`/decide`, `/track`) используют кеши и индексы, а отчёты/guardrails работают по оконной выборке. | Документация. | `готово` |
| `B9-7` | `3.7/B9` | Отсутствие мер по эффективности хранения/чтения делает систему плохо масштабируемой. | `docs/OPERATIONS.md` (раздел 5), описание индексов и ключей кеша | Проверить, что в БД есть индексы по `experiment_id`, `variant_id`, `decision_id`, `event_type_key`, `timestamp`, а кеши Redis снижают нагрузку на БД. | Схема БД (миграции) и документация. | `готово` |
| `B10-1` | `D.5/B10` | Без автоматизированного линтинга стиль и базовые ошибки зависят от человека. | Цель `lint` в `src/Makefile` (`go vet ./...`) | Запустить `make lint` и убедиться, что команда завершится успешно и может быть добавлена в CI. | Go toolchain, доступ к `src/`. | `готово` |
| `B10-2` | `D.5/B10` | Ручное форматирование кода делает диффы шумными и усложняет ревью. | Цель `fmt` в `src/Makefile` (`go fmt ./...`) | Запустить `make fmt` и убедиться, что команда форматирует код по стандарту Go. | Go toolchain. | `готово` |
| `FX-1` | `7.1..7.5` / `8.1..8.4` | Без допфич сложно показать зрелость платформы: уведомления и UI для инсайтов существенно повышают практическую ценность решения. | Платформа уведомлений: `docs/ARCHITECTURE.md` (Notifications), `docs/OPERATIONS.md`, `docker-compose.yml` (NOTIFY_*); Experiment Insights UI: `ui/experiment-insights`, `docs/EXPERIMENT_INSIGHTS_UI.md` | Для уведомлений: настроить guardrail и получить Telegram‑/Email‑нотификацию при срабатывании (см. runbook, раздел 5). Для UI: запустить `http://localhost/`, вставить JWT и показать список/деталь эксперимента с метриками, guardrails и экспортом. | Запущенный стек, Telegram‑бот и/или email‑лог, браузер с доступом к `http://localhost/`. | `готово` |
| `FX-2` | `7.1..7.5` / `8.1..8.4` | Если не задокументировать ограничения допфич, их начнут использовать вне допустимого диапазона (спам‑уведомления, ожидание полного админ‑UI и т.д.). | Ограничения Notifications и UI в `docs/ARCHITECTURE.md`, `docs/OPERATIONS.md`, `docs/EXPERIMENT_INSIGHTS_UI.md` | Проверить, что в документации явно указано: уведомления поддерживают ограниченный набор каналов и имеют rate‑limit/dedup, а UI — это read‑only панель инсайтов (без создания и управления экспериментами). На демо это проговаривается. | Только репозиторий и запущенные допфичи; дополнительных данных не требуется. | `готово` |

